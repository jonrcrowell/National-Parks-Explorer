import React from 'react'
import styled from 'styled-components'

const Ul = styled.ul`
/* Adapt the colors based on primary prop */

font-size: .5em;
margin: .25em;
padding: 0.25em 1em;
text-align: left;
`;

export default Ul;